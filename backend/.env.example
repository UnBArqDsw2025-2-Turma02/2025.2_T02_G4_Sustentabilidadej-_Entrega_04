DATABASE_URL="file:./dev.db"
PORT=3001
JWT_SECRET="change-me-to-a-strong-secret"

# SMTP (configure with your provider; for local dev you can use https://ethereal.email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Relatórios Consumo <no-reply@example.com>"

# Cron: every month on the 1st at 08:00
REPORT_CRON_SCHEDULE="0 8 1 * *"
